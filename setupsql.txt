CREATE TABLE IF NOT EXISTS users(
id BIGINT PRIMARY KEY,
class TEXT,
name TEXT);

CREATE TABLE IF NOT EXISTS bwl(
name TEXT PRIMARY KEY,
class TEXT,
id BIGINT);

CREATE TABLE IF NOT EXISTS mc(
name TEXT PRIMARY KEY,
class TEXT,
id BIGINT);